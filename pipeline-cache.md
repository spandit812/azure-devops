If you have multiple projets having same packages, you can use cache to reduce build pipeline timing.  
For example: if you have dotnet project to install nuget packages, you can use caches in the pipeline.  

